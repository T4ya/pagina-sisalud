---
---
<section id="contact" class="py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-display font-bold mb-4">Contacte con <span class="text-primary">Nosotros</span></h2>
      <p class="text-gray-600 max-w-2xl mx-auto">¿Tiene preguntas sobre nuestros servicios o desea colaborar? Póngase en contacto con nosotros utilizando el formulario a continuación.</p>
    </div>
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <div class="bg-light rounded-2xl p-8 shadow-lg" data-aos="fade-right">
        <h3 class="text-2xl font-bold mb-6">Información de Contacto</h3>
        
        <div class="space-y-6">
          <div class="flex items-start gap-4">
            <div class="bg-primary rounded-full p-3 text-white mt-1">
              <i class="fas fa-map-marker-alt"></i>
            </div>
            <div>
              <h4 class="font-bold text-lg">Dirección</h4>
              <p class="text-gray-600">Carrera 15 #78-33, Bogotá, Colombia</p>
            </div>
          </div>
          
          <div class="flex items-start gap-4">
            <div class="bg-primary rounded-full p-3 text-white mt-1">
              <i class="fas fa-envelope"></i>
            </div>
            <div>
              <h4 class="font-bold text-lg">Correo Electrónico</h4>
              <p class="text-gray-600">info@laboratoriosalisalud.com</p>
            </div>
          </div>
          
          <div class="flex items-start gap-4">
            <div class="bg-primary rounded-full p-3 text-white mt-1">
              <i class="fas fa-phone"></i>
            </div>
            <div>
              <h4 class="font-bold text-lg">Teléfono</h4>
              <p class="text-gray-600">+57 (601) 123-4567</p>
            </div>
          </div>
        </div>
        
        <div class="mt-10">
          <h4 class="font-bold text-lg mb-4">Síganos</h4>
          <div class="flex gap-4">
            <a href="https://twitter.com/labalisalud" target="_blank" rel="noopener noreferrer" class="bg-primary hover:bg-secondary text-white p-3 rounded-full transition-all">
              <i class="fab fa-twitter"></i>
            </a>
            <a href="https://facebook.com/labalisalud" target="_blank" rel="noopener noreferrer" class="bg-primary hover:bg-secondary text-white p-3 rounded-full transition-all">
              <i class="fab fa-facebook-f"></i>
            </a>
            <a href="https://linkedin.com/company/laboratorios-alisalud" target="_blank" rel="noopener noreferrer" class="bg-primary hover:bg-secondary text-white p-3 rounded-full transition-all">
              <i class="fab fa-linkedin-in"></i>
            </a>
            <a href="https://instagram.com/labalisalud" target="_blank" rel="noopener noreferrer" class="bg-primary hover:bg-secondary text-white p-3 rounded-full transition-all">
              <i class="fab fa-instagram"></i>
            </a>
          </div>
        </div>
        
        <!-- Mapa de ubicación con coordenadas específicas -->
        <div class="mt-10 rounded-xl overflow-hidden shadow-md" data-aos="fade-up">
          <h4 class="font-bold text-lg mb-4">Nuestra Ubicación</h4>
          <div class="h-64 w-full">
            <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3976.6738272001087!2d-74.05672702427518!3d4.6668396960595275!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8e3f9a3b2befed5f%3A0x5dd02c1f8e7d7f2c!2sCra.%2015%20%2378-33%2C%20Bogot%C3%A1%2C%20Colombia!5e0!3m2!1ses!2sus!4v1709420834872!5m2!1ses!2sus" 
              width="100%" 
              height="100%" 
              style="border:0;" 
              allowfullscreen="" 
              loading="lazy"
              class="rounded-lg">
            </iframe>
          </div>
        </div>
      </div>
      
      <div data-aos="fade-left">
        <form id="contact-form" class="space-y-6">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Nombre</label>
              <input 
                type="text" 
                id="name" 
                name="name" 
                class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-primary transition-all"
                placeholder="Su nombre"
                required
              />
            </div>
            
            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Correo Electrónico</label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-primary transition-all"
                placeholder="su.correo@ejemplo.com"
                required
              />
            </div>
          </div>
          
          <div>
            <label for="subject" class="block text-sm font-medium text-gray-700 mb-1">Asunto</label>
            <input 
              type="text" 
              id="subject" 
              name="subject" 
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-primary transition-all"
              placeholder="¿Cómo podemos ayudarle?"
              required
            />
          </div>
          
          <div>
            <label for="message" class="block text-sm font-medium text-gray-700 mb-1">Mensaje</label>
            <textarea 
              id="message" 
              name="message" 
              rows="5" 
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-primary transition-all"
              placeholder="Su mensaje aquí..."
              required
            ></textarea>
          </div>
          
          <div>
            <button 
              type="submit" 
              class="w-full bg-primary hover:bg-secondary text-white px-6 py-3 rounded-lg font-medium transition-all"
            >
              Enviar Mensaje
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('astro:page-load', () => {
    const form = document.getElementById('contact-form');
    
    if (form) {
      form.addEventListener('submit', (e) => {
        e.preventDefault();
        
        // Obtener valores del formulario con tipado correcto
        const nameInput = document.getElementById('name') as HTMLInputElement;
        const emailInput = document.getElementById('email') as HTMLInputElement;
        const subjectInput = document.getElementById('subject') as HTMLInputElement;
        const messageInput = document.getElementById('message') as HTMLTextAreaElement;
        
        // Verificar que los elementos existen antes de acceder a sus valores
        const name = nameInput?.value || '';
        const email = emailInput?.value || '';
        const subject = subjectInput?.value || '';
        const message = messageInput?.value || '';
        
        // Aquí normalmente enviaría los datos del formulario a un servidor
        // Para fines de demostración, solo lo registramos y mostramos un mensaje de éxito
        console.log({ name, email, subject, message });
        
        // Mostrar mensaje de éxito
        if (form) {
          form.innerHTML = `
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
              <strong class="font-bold">¡Gracias!</strong>
              <span class="block sm:inline"> Su mensaje ha sido enviado con éxito. Nos pondremos en contacto con usted pronto.</span>
            </div>
          `;
        }
      });
    }
  });
</script>